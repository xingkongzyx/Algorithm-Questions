/* 

## LeetCode 632 超详细新手讲解

---

### 第一步：理解题目到底在问什么

假设你有 3 个**已排好序**的数字列表：

```
列表0: [4, 10, 15, 24, 26]
列表1: [0,  9, 12, 20]
列表2: [5, 18, 22, 30]
```

题目要求：找到一个**最小的区间** `[a, b]`，使得**每个列表里都至少有一个数字**落在 `a` 和 `b` 之间（包括 a 和 b）。

#### 什么是"区间"？

区间 `[a, b]` 就是从 a 到 b 的一段数轴范围。比如 `[5, 15]` 表示 5 ≤ x ≤ 15 的所有数。

#### 什么叫"覆盖每个列表"？

就是说这个区间里，**每个列表都能找到至少一个数**。

举几个例子：

| 区间 | 列表0 有数在内？ | 列表1 有数在内？ | 列表2 有数在内？ | 合格？ | 区间大小 |
|---|---|---|---|---|---|
| `[0, 26]` | ✅ (4,10,15,24,26) | ✅ (0,9,12,20) | ✅ (5,18,22,30) | ✅ | 26 (太大) |
| `[5, 15]` | ✅ (10,15) | ✅ (9,12) | ✅ (5) | ✅ | 10 |
| `[10, 12]` | ✅ (10) | ✅ (12) | ❌ (没有) | ❌ | — |
| `[20, 24]` | ✅ (24) | ✅ (20) | ✅ (22) | ✅ | **4** ← 最优！ |

我们要找的就是**区间大小最小**且仍然合格的那个。答案是 `[20, 24]`。

---

### 第二步：暴力想法（先理解，再优化）

最笨的方法：从每个列表各选一个数字，看这些数字的范围有多大。

```
从列表0选一个，从列表1选一个，从列表2选一个
比如选 4, 0, 5 → 范围 = max(4,0,5) - min(4,0,5) = 5 - 0 = 5
比如选 24, 20, 22 → 范围 = 24 - 20 = 4  ← 更好！
```

为什么这样做是对的？因为：
- 你从每个列表各选了一个数 → 这些数一定各自属于自己的列表
- 用这些数的 `[最小值, 最大值]` 作为区间 → 这个区间一定覆盖了每个列表

但暴力枚举所有组合太慢了！如果每个列表有 n 个元素，K 个列表，就要枚举 $n^K$ 种组合。

---

### 第三步：聪明的方法 — 最小堆

#### 核心想法

我们给每个列表安排一个**指针**（像手指一样指着列表中的某个位置），初始都指向各自列表的**第一个元素**（最小的）。

```
列表0: [👉4, 10, 15, 24, 26]    ← 指针指向 4
列表1: [👉0,  9, 12, 20]        ← 指针指向 0
列表2: [👉5, 18, 22, 30]        ← 指针指向 5
```

当前选中的3个数：`4, 0, 5`
- 最小值 = 0（来自列表1）
- 最大值 = 5
- 区间 = `[0, 5]`，大小 = 5

#### 关键问题：怎么缩小区间？

区间 = 最大值 - 最小值。要缩小它，有两种方式：
1. **让最大值变小** → 但列表是升序的，往后走只会更大，没法让最大值变小
2. **让最小值变大** → ✅ 把最小值所在列表的指针**往右移一格**，换一个更大的数

所以策略是：**每次都把当前最小的那个数淘汰掉，让它的指针前进一步。**

#### 逐步演示

**第1轮：**
```
选中: 4(列表0), 0(列表1), 5(列表2)
最小值 = 0 (列表1)，最大值 = 5
区间 [0, 5]，大小 = 5
→ 淘汰最小值 0，列表1 指针前进
```

**第2轮：**
```
列表0: [👉4, 10, 15, 24, 26]
列表1: [0, 👉9, 12, 20]         ← 指针移到了 9
列表2: [👉5, 18, 22, 30]

选中: 4, 9, 5
最小值 = 4 (列表0)，最大值 = 9
区间 [4, 9]，大小 = 5
→ 淘汰最小值 4，列表0 指针前进
```

**第3轮：**
```
列表0: [4, 👉10, 15, 24, 26]    ← 指针移到了 10
列表1: [0, 👉9, 12, 20]
列表2: [👉5, 18, 22, 30]

选中: 10, 9, 5
最小值 = 5 (列表2)，最大值 = 10
区间 [5, 10]，大小 = 5
→ 淘汰最小值 5，列表2 指针前进
```

**第4轮：**
```
列表0: [4, 👉10, 15, 24, 26]
列表1: [0, 👉9, 12, 20]
列表2: [5, 👉18, 22, 30]       ← 指针移到了 18

选中: 10, 9, 18
最小值 = 9 (列表1)，最大值 = 18
区间 [9, 18]，大小 = 9
→ 淘汰最小值 9，列表1 指针前进
```

**第5轮：**
```
选中: 10, 12, 18
最小值 = 10，最大值 = 18
区间 [10, 18]，大小 = 8
→ 淘汰 10，列表0 前进
```

**第6轮：**
```
选中: 15, 12, 18
区间 [12, 18]，大小 = 6
→ 淘汰 12，列表1 前进
```

**第7轮：**
```
选中: 15, 20, 18
区间 [15, 20]，大小 = 5
→ 淘汰 15，列表0 前进
```

**第8轮：**
```
选中: 24, 20, 18
区间 [18, 24]，大小 = 6
→ 淘汰 18，列表2 前进
```

**第9轮：**
```
选中: 24, 20, 22
区间 [20, 24]，大小 = 4  ← 🎉 目前最优！
→ 淘汰 20，列表1 前进
```

**第10轮：**
```
列表1 已经没有下一个元素了！→ 停止
```

**最终答案：`[20, 24]`** ✅

#### 用什么数据结构快速找"当前最小值"？

每次都要在 K 个数中找最小值 → 用**最小堆（Min Heap）**！

- 堆中始终保持 K 个元素（每个列表一个）
- `heap.pop()` 就能 $O(\log K)$ 取出最小值
- 插入新元素也是 $O(\log K)$

#### 最大值怎么维护？

用一个变量 `curMax` 单独记录。每次有新元素入堆时，更新 `curMax = Math.max(curMax, 新元素)`。
```
区间 = [左端点, 右端点]
         ↑          ↑
       堆顶给出    curMax 给出
       (自动维护)   (手动维护)
```

---
### 第四步：总结

```mermaid
flowchart LR
    A["每个列表各出一个指针<br/>初始指向首元素"] --> B["用最小堆<br/>快速找到最小值"]
    B --> C["区间 = [最小值, 最大值]<br/>尝试更新答案"]
    C --> D["淘汰最小值<br/>该列表指针前进一步"]
    D --> E{"该列表还有元素？"}
    E -- 有 --> B
    E -- 没有 --> F["结束，返回最优区间"]
```

| 概念 | 说明 |
|---|---|
| **为什么用堆？** | 快速找到 K 个当前元素中的最小值，时间 $O(\log K)$ |
| **为什么淘汰最小值？** | 因为列表是升序的，替换最小值为更大的数才可能缩小区间 |
| **为什么某个列表耗尽就停？** | 因为区间必须覆盖**每个**列表，少了一个就不合格 |
| **时间复杂度** | $O(N \log K)$，N 是所有元素总数，K 是列表个数 |
| **空间复杂度** | $O(K)$，堆中始终只有 K 个元素 |

*/

/*
## 为什么需要 curMax？

### 一句话回答

**因为我们需要知道区间的右端点是什么，而最小堆只能告诉我们最小值（左端点），无法告诉我们最大值（右端点）。**

---

### 回到最基本的需求

每一步我们要计算区间大小 = `最大值 - 最小值`。

- **最小值**：堆顶直接给出 → ✅ 不需要额外变量
- **最大值**：堆不提供这个信息 → ❌ 必须自己记录

如果没有 `curMax`，你只知道最小值是多少，但不知道最大值是多少，就**算不出区间大小**。

---

### 用具体例子说明

```
堆中有 3 个数: [5, 10, 18]
```

堆顶告诉你：最小值 = 5

但你要计算区间 `[5, ?]`，这个 `?` 是多少？你不知道！

你当然可以遍历堆中所有元素找最大值，但那需要 $O(K)$ 时间，每一步都这样做太慢了。

所以我们用 `curMax` 这个变量来**记住**最大值 = 18，这样就能直接算出：

```
区间 = [5, 18]，大小 = 18 - 5 = 13
```

---

### 为什么一个简单变量就够了？（不需要最大堆）

因为 `curMax` 有一个特殊性质：**它只会变大或不变，永远不会变小。**

每一步操作：
1. 弹出最小值（比如 5）
2. 放入一个新值（比如 22）

此时：
- 产生旧 `curMax`（18）的那个元素**没有被弹出**，它还在堆里
- 新放入的 22 比 18 大 → `curMax` 更新为 22

或者新放入的是 12（比 18 小）→ `curMax` 还是 18，不变。

**无论如何都不会变小**，所以只要 `curMax = Math.max(curMax, 新值)` 就行了。

---

### 如果不用 curMax 会怎样？

你只有两个选择：

| 方案 | 做法 | 代价 |
|---|---|---|
| **用 curMax 变量** | 每次入堆时 `Math.max` 更新 | $O(1)$ |
| **每次遍历堆找最大值** | 扫描堆数组找 max | $O(K)$，总复杂度变为 $O(NK)$ |

用 `curMax` 变量就是一个 **$O(1)$ 的优化**，利用了"最大值只增不减"这个性质。

*/

var smallestRange = function (nums) {
    const heap = new MyMinHeap();
    const k = nums.length; // K：列表的个数

    // result 记录当前找到的最优区间 [左端点, 右端点]
    // 初始化为 [-Infinity, Infinity]，保证任何有效区间都比它小
    let result = [-Infinity, Infinity];
    // curMax 追踪堆中所有元素的最大值（即当前区间的右端点）
    let curMax = -Infinity;

    // ===== 第一步：初始化 =====
    // 将每个列表的第一个元素（最小值）入堆，同时记录它们中的最大值
    // 堆中每个节点存储: [元素值, 该元素所在列表的索引, 该元素在列表中的索引]
    for (let i = 0; i < k; i++) {
        heap.push(nums[i][0], i, 0);
        curMax = Math.max(curMax, nums[i][0]);
    }
    // 此时堆中有 K 个元素，分别来自 K 个列表
    // 当前区间 = [堆顶最小值, curMax]

    // ===== 第二步：不断缩小区间 =====
    while (true) {
        // 弹出堆顶（当前所有指针指向的元素中的最小值）
        // curMin = 当前最小值（区间左端点）
        // listIdx = 这个最小值来自第几个列表
        // elemIdx = 这个最小值在该列表中的索引位置
        const [curMin, listIdx, elemIdx] = heap.pop();

        // 当前区间为 [curMin, curMax]，如果比已记录的最优区间更小，则更新
        if (curMax - curMin < result[1] - result[0]) {
            result = [curMin, curMax];
        }

        // 如果最小值所在的列表已经没有下一个元素了，必须停止
        // 因为继续下去无法保证每个列表都有元素在区间内
        const nextElemIdx = elemIdx + 1;
        if (nextElemIdx >= nums[listIdx].length) {
            break;
        }

        // 将最小值所在列表的下一个元素（更大的值）加入堆中
        // 相当于把该列表的指针往右移一格，尝试让最小值变大从而缩小区间
        const nextVal = nums[listIdx][nextElemIdx];
        heap.push(nextVal, listIdx, nextElemIdx);

        // 新入堆的元素可能比当前最大值还大，需要更新 curMax
        curMax = Math.max(curMax, nextVal);
    }

    return result;
};

class MyMinHeap {
    constructor() {
        this.data = [];
        this.size = 0;
    }

    push(eleVal, listIdx, eleIdx) {
        this.data.push([eleVal, listIdx, eleIdx]);
        this.size++;
        this.siftUp(this.size - 1);
    }

    siftUp(curIdx) {
        while (curIdx > 0) {
            let parentIdx = Math.floor((curIdx - 1) / 2);
            if (this.data[curIdx][0] < this.data[parentIdx][0]) {
                this.swap(curIdx, parentIdx);
                curIdx = parentIdx;
            } else {
                break;
            }
        }
    }

    pop() {
        this.swap(0, this.size - 1);
        let ele = this.data.pop();
        this.size--;
        this.siftDown(0);
        return ele;
    }

    siftDown(curIdx) {
        while (2 * curIdx + 1 < this.size) {
            let childOneIdx = 2 * curIdx + 1;
            let childTwoIdx = 2 * curIdx + 2;
            let idxToSwap = childOneIdx;

            if (
                childTwoIdx < this.size &&
                this.data[childTwoIdx][0] < this.data[idxToSwap][0]
            ) {
                idxToSwap = childTwoIdx;
            }

            if (this.data[curIdx][0] > this.data[idxToSwap][0]) {
                this.swap(curIdx, idxToSwap);
                curIdx = idxToSwap;
            } else {
                break;
            }
        }
    }

    swap(idx1, idx2) {
        let temp = this.data[idx1];
        this.data[idx1] = this.data[idx2];
        this.data[idx2] = temp;
    }
}
